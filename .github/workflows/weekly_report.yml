name: 🏠 분양정보 유망도 리포트

on:
  workflow_dispatch:
    # GitHub UI에서 수동 실행 가능

env:
  PYTHON_VERSION: '3.14'
  WORKING_DIR: housing

jobs:
  generate-and-send-report:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}

    defaults:
      run:
        working-directory: ${{ env.WORKING_DIR }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check execution time
        run: |
          echo "✅ Workflow started"
          echo "UTC: $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo "KST: $(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M:%S')"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: ${{ env.WORKING_DIR }}/requirements.txt

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate housing report
        id: run_report
        run: |
          python main.py all 2>&1
          mkdir -p _site/housing
          cp output/report.html _site/housing/index.html
          echo "report_path=output/report.html" >> $GITHUB_OUTPUT
          ls -la output/report.html _site/housing/index.html
        env:
          DATA_GO_KR_API_KEY: ${{ secrets.DATA_GO_KR_API_KEY }}
          VWORLD_API_KEY: ${{ secrets.VWORLD_API_KEY }}

      - name: Deploy to GitHub Pages
        if: success()
        uses: actions/upload-pages-artifact@v3
        with:
          path: housing/_site

      - name: Deploy to Pages
        id: deploy
        if: success()
        uses: actions/deploy-pages@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Send email
        uses: dawidd6/action-send-mail@v3
        if: always()
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: >-
            ${{ job.status == 'success' && '✅' || '⚠️' }}
            분양정보 유망도 주간리포트
            (${{ steps.run_report.outputs.report_path != '' && 'OK' || 'FAILED' }})
          to: ${{ secrets.EMAIL_TO }}
          from: 박기영
          html_body: file://housing/output/report.html

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: housing-report
          path: ${{ env.WORKING_DIR }}/output/report.html
